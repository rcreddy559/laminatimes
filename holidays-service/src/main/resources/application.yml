server:
  port: 8088

spring:
  application:
    name: holidays-service
  cloud:
    config:
      uri: http://CONFIG-SERVER

logging:
  file:
    name: /Users/ravi/Desktop/logs/holidays-service/holidays-service.log

kafka:
  boot:
    server: localhost:9092
  topic:
    name: myTopic-kafkasender
  consumer:
    group:
      id: lamina-group-id